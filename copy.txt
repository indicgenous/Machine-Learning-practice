model:
  name: pspnet
  backbone: resnet50
  pretrained: true
  num_classes: 20  # Replace this with the actual number of classes in your dataset

dataset:
  name: bdd100k
  root: /path/to/PSPNet/datasets/bdd100k  # Update with your dataset path
  train:
    images: /path/to/PSPNet/datasets/bdd100k/images/train
    annotations: /path/to/PSPNet/datasets/bdd100k/labels/train
  val:
    images: /path/to/PSPNet/datasets/bdd100k/images/val
    annotations: /path/to/PSPNet/datasets/bdd100k/labels/val

testing:
  test_images: /path/to/PSPNet/datasets/bdd100k/images/test  # Path to test images
  results_dir: ./results  # Directory to save test results

training:
  batch_size: 16  # Adjust based on your GPU memory
  num_epochs: 50  # Number of training epochs
  learning_rate: 0.001  # Base learning rate
  weight_decay: 0.0001
  lr_scheduler:
    name: poly
    power: 0.9

onnx:
  export_path: ./onnx_models  # Path to save ONNX models
  opset_version: 11  # ONNX opset version

logging:
  log_interval: 10  # Log every N batches
  save_dir: ./checkpoints  # Directory to save model checkpoints
  save_interval: 5  # Save a checkpoint every N epochs
